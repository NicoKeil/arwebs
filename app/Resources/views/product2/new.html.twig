{% extends 'base.html.twig' %}

{% block body %}
    <h1>Product2 creation</h1>

    {{ form_start(form, {'enctype': 'multipart/form-data'}) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('product2_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}

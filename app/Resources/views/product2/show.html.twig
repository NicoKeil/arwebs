{% extends 'base.html.twig' %}

{% block body %}
    <h1>Product2</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product2.id }}</td>
            </tr>
            <tr>
                <th>Categoryid</th>
                <td>{{ product2.categoryId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product2.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ product2.image }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('product2_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('product2_edit', { 'id': product2.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
